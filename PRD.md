# PRD — 내 예산으로 서울 어디?

서울 아파트 실거래가 탐색 대시보드

- **작성일**: 2026-08-12
- **배포 대상**: GitHub Pages (https://gjdbqls250-arch.github.io/sesac-web/)
- **데이터 출처**: 서울열린데이터광장 서울시 부동산 실거래가 정보(OA-21275)

---

## 1. 페르소나

**전세를 구하는 사회초년생**

서울에서 첫 독립을 준비한다. 모아둔 돈과 대출 한도를 합쳐 예산은 정해져 있는데,
그 돈으로 어느 동네에 갈 수 있는지 감이 없다. 부동산 앱은 매물 광고 위주라
"실제로 얼마에 거래됐는지"를 알기 어렵다.

### 핵심 질문

> "내 예산으로 서울 어디에 갈 수 있나?"

### 사용 맥락

- 퇴근 후 노트북으로, 예산 슬라이더를 이리저리 움직여보며 탐색
- 결론을 내리는 도구가 아니라 **후보 동네를 좁히는** 도구
- 결과를 캡처해 친구/가족에게 공유

---

## 2. 제품 원칙

| 원칙 | 의미 |
|---|---|
| **실거래만 보여준다** | 호가·광고가 아닌 실제 체결된 거래. 추정·예측하지 않는다 |
| **탐색형** | 정답을 제시하지 않고, 사용자가 직접 돌려보며 발견하게 한다 |
| **즉시 반응** | 필터 조작 시 지연 없이 지도와 리스트가 갱신된다 |
| **데이터 한계를 숨기지 않는다** | 표본이 적은 구간은 "거래 3건" 처럼 건수를 함께 노출한다 |

---

## 3. 데이터 범위

| 항목 | 값 |
|---|---|
| 총 거래 | 320,141건 |
| 기간 | 2025-07 ~ 2026-06 (12개월) |
| 지역 | 서울 25개 자치구 전체 |
| 거래유형 | 매매 72,459 / 전세 129,649 / 월세 118,033 |
| 필드 | 자치구, 법정동, 단지명, 면적(㎡), 층, 거래유형, 가격, 보증금, 월세, 평당가 |

> **주의**: 단지명에 쉼표가 포함된 레코드(`"...5동, 11동"`)가 존재한다.
> 단순 문자열 분할로 파싱하면 필드가 밀려 집계가 틀어지므로 반드시 CSV 파서를 사용할 것.

### 데이터 특성 (기획 반영 사항)

- 전세 보증금 중앙값 5.6억 → 사회초년생 예산대와 괴리가 크다.
  **60㎡ 이하 소형 전세가 54,320건** 존재하므로 면적 필터가 필수.
- 전세 보증금 범위: 1,000만원 ~ 138.5억 → 슬라이더는 **비선형(로그) 스케일** 또는
  실사용 구간(1억~10억)에 해상도를 집중해야 한다.
- 월세 레코드는 보증금 + 월세가 함께 존재 → 단일 금액으로 비교 불가.
  **환산보증금**(보증금 + 월세×100)을 파생 지표로 계산해 비교 축을 통일한다.

---

## 4. 기능 요구사항

### 4.1 필터 (사용자 입력)

| 필터 | UI | 기본값 | 비고 |
|---|---|---|---|
| **거래유형** | 세그먼트 토글 (전세/월세/매매) | 전세 | 유형 전환 시 예산 슬라이더 범위도 함께 전환 |
| **예산** | 슬라이더 + 직접입력 | 전세 3억 | 상한만 지정 (이 금액 **이하**) |
| **면적** | 범위 슬라이더 (평) | 전체 | ㎡ ↔ 평 병기 표시 |
| **자치구** | 멀티 선택 칩 | 전체 | 지도 클릭과 양방향 동기화 |
| **거래 시점** | 세그먼트 (최근 3/6/12개월) | 12개월 | |
| **층수** | 체크박스 | 전체 | "1층·반지하 제외" 단일 옵션 |

- 필터 상태는 **URL 쿼리스트링에 반영** → 링크 공유 시 동일 화면 재현

### 4.2 지도 (서울 25구)

- 실제 행정구역 경계 기반 SVG (외부 지도 API 미사용)
- **색상 기준 토글** (2종):
  1. **가능 매물 수** — 현재 필터를 통과한 거래 건수 (기본값)
  2. **시세 중앙값** — 현재 필터 유형의 가격 중앙값
- 구에 hover → 툴팁 (구 이름 / 건수 / 중앙값)
- 구 클릭 → 해당 구 선택 토글, 리스트가 해당 구로 필터링
- 색상 스케일 범례 필수

### 4.3 결과 리스트

필터를 통과한 **실제 거래 목록**을 그대로 노출한다.

| 컬럼 | 표기 |
|---|---|
| 단지명 | 굵게 |
| 위치 | 자치구 · 법정동 |
| 면적 | 84.2㎡ (25.5평) |
| 층 | 9층 |
| 금액 | 전세: 5.3억 / 월세: 1억 + 45만 / 매매: 15.6억 |
| 계약일 | 2026-05-20 |

- 정렬: 금액 낮은순(기본) / 높은순 / 최신순 / 면적 넓은순
- 가상 스크롤 또는 페이지네이션 (결과가 수만 건일 수 있음)
- 상단에 **"조건에 맞는 거래 N건"** 카운트 표시

### 4.4 요약 카드

필터 적용 결과의 한 줄 요약 (리스트 위):

- 조건에 맞는 거래 **N건** · 최저 **X억** · 중앙값 **Y억**
- 가장 매물이 많은 동네: **○○구 ○○동 (N건)**
- 결과 0건일 때: 예산을 얼마까지 올리면 결과가 나오는지 안내

---

## 5. 비기능 요구사항

| 항목 | 목표 |
|---|---|
| 초기 로드 | 요약 데이터만 로드, 2초 이내 인터랙션 가능 |
| 구 상세 로드 | 클릭 시 on-demand, 500ms 이내 |
| 필터 반응 | 100ms 이내 (클라이언트 사이드 연산) |
| 반응형 | 모바일(375px) ~ 데스크톱(1440px) |
| 호스팅 | GitHub Pages 정적 호스팅, 빌드 서버 없음 |
| 디자인 | Montage(wanteddev) 토큰 계승 — Pretendard, blue-50 `#0066FF`, neutral 스케일 |

---

## 6. 기술 아키텍처

### 6.1 제약과 해법

**제약**: 원본 CSV 27MB / 320k행. GitHub Pages는 정적 파일만 제공.

**해법**: 빌드 타임 전처리로 데이터를 분할하고, 구 단위 on-demand 로드.

```
원본 CSV (27MB)
   │
   ├─ build_data.py (빌드 타임 1회 실행)
   │
   ├──> data/summary.json      전 구 집계 (지도 초기 렌더용, ~50KB)
   ├──> data/seoul-map.json    25구 SVG path (~40KB)
   └──> data/gu/{code}.json    구별 거래 25개 파일 (각 gzip 후 50~180KB)
```

검증 결과: 최대 구(노원구 25,868건) 기준 raw 874KB → **gzip 176KB**. 수용 가능.

### 6.2 데이터 계약 (Data Contract)

구현 병렬화를 위해 스키마를 여기서 확정한다.

#### `data/summary.json`

```json
{
  "meta": {
    "totalRows": 320141,
    "periodStart": "2025-07",
    "periodEnd": "2026-06",
    "builtAt": "2026-08-12"
  },
  "gu": [
    {
      "code": "11110",
      "name": "노원구",
      "counts": { "매매": 7190, "전세": 10557, "월세": 8121 },
      "median": { "매매": 60000, "전세": 29000, "월세": 11773 }
    }
  ]
}
```

- `median` 단위는 **만원**. 월세의 median은 환산보증금(보증금 + 월세×100) 기준.
- **`code`는 표준 행정구역 코드가 아니라 사용하는 GeoJSON(`southkorea/seoul-maps` KOSTAT 2013)의
  `properties.code`를 그대로 따른다** (노원구 = `11110`). `summary.json`, `gu/{code}.json` 파일명,
  `seoul-map.json`이 모두 동일한 코드 체계를 써야 조인이 성립한다.

#### `data/gu/{code}.json`

문자열 인터닝 + 배열 인코딩으로 용량 최소화.

```json
{
  "code": "11350",
  "name": "노원구",
  "dongs": ["중계동", "상계동", "..."],
  "complexes": ["동진신안", "대림벽산(363)", "..."],
  "rows": [
    [0, 0, 135.0, 11, 0, 156000, 0, "260520"]
  ]
}
```

**`rows` 배열 인덱스 정의** (순서 고정):

| # | 필드 | 타입 | 설명 |
|---|---|---|---|
| 0 | dongIdx | int | `dongs` 배열 인덱스 |
| 1 | complexIdx | int | `complexes` 배열 인덱스 |
| 2 | areaM2 | float | 전용면적 ㎡ (소수 1자리) |
| 3 | floor | int | 층 (지하는 음수, 불명은 0) |
| 4 | dealType | int | `0=매매, 1=전세, 2=월세` |
| 5 | amount | int | 만원. 매매=거래가, 전세·월세=보증금 |
| 6 | monthlyRent | int | 만원. 월세만 사용, 그 외 `0` |
| 7 | date | string | `YYMMDD` 6자리 |

#### `data/seoul-map.json`

```json
{
  "viewBox": "0 0 800 600",
  "districts": [
    { "code": "11350", "name": "노원구", "path": "M123.4,56.7L...Z", "cx": 240.1, "cy": 88.3 }
  ]
}
```

- GeoJSON(`southkorea/seoul-maps`, KOSTAT 2013)을 **메르카토르 투영 후 정규화**하여 SVG path로 변환
- `cx`/`cy`는 구 이름 라벨 배치 좌표 (폴리곤 중심)

### 6.3 파생 지표 정의

| 지표 | 계산식 | 용도 |
|---|---|---|
| 환산보증금 | `보증금 + 월세 × 100` | 월세 거래를 단일 금액축으로 비교 |
| 평 | `㎡ ÷ 3.3058` | 면적 표기 병기 |

### 6.4 파일 구조

```
/
├── index.html              대시보드 (단일 페이지)
├── assets/
│   ├── app.js              필터·상태·렌더링 로직
│   └── style.css           Montage 토큰 기반 스타일
├── data/
│   ├── summary.json
│   ├── seoul-map.json
│   └── gu/11110.json ... 11740.json   (25개)
├── build_data.py           CSV → JSON 전처리 (빌드 타임)
├── analyze.py              (기존) 노원구 분석 스크립트
├── seoul-apt-latest.csv    원본 데이터
└── PRD.md
```

- 프레임워크 없이 **바닐라 JS**. 빌드 스텝이 없어 GitHub Pages에 그대로 배포된다.

---

## 7. 범위 밖 (Out of Scope)

명시적으로 이번 버전에서 제외한다.

- 로그인 / 즐겨찾기 저장 (서버 없음)
- 가격 예측 · 투자 추천 (실거래 사실만 전달한다는 원칙)
- 지하철역·학군 등 외부 데이터 결합
- 개별 단지 상세 페이지
- 자기소개 페이지 (대시보드 단일 페이지로 결정)

---

## 8. 완료 기준 (Definition of Done)

- [ ] 전세 3억 이하 · 20평 이상으로 필터 시 결과가 정상 노출된다
- [ ] 지도 25개 구가 모두 렌더링되고, 색상 기준 토글이 동작한다
- [ ] 구 클릭 ↔ 자치구 칩 선택이 양방향 동기화된다
- [ ] 필터 조작 후 URL이 갱신되고, 그 URL로 재접속 시 동일 화면이 복원된다
- [ ] 모바일 375px에서 가로 스크롤이 발생하지 않는다
- [ ] 결과 0건일 때 안내 문구가 표시된다
- [ ] GitHub Pages 배포 후 실제 URL에서 동작이 확인된다
